# Denial Attack Quiz update validation

Changes applied:

- Removed the visible static-website/backend note from the quiz onboarding panel.
- Reworded the local high-score helper text so it simply states that scores are saved on the current device.
- Removed the backend-ready notice from the high-score panel.
- Updated the Buy me a coffee link to `https://buymeacoffee.com/markdorsett`.

Validation checks:

- `game.html` contains the updated Buy me a coffee URL.
- `game.html` no longer contains the visible phrases `Static website note` or `This static version`.
- No JavaScript changes were required for this update.
